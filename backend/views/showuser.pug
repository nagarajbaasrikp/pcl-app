extends layout

block content
    div
        h1 User Details
        h2 Name: #{name}
        p email: #{email}
        p Username: #{username}
        - const imgAttributes  =  { "src": imgPath, "alt": "Error loading image" }
        img&attributes(imgAttributes)